<h1><%= @song.title %></h1>

<p><strong>Release Year:</strong> <%= @song.release_date %></p>
<p><strong>Genre:</strong> <%= @song.genre %></p>
<p><strong>Album:</strong> <%= @song.album %></p>
<p><strong>Artist:</strong> <%= @song.artist.name %></p>
<%= image_tag @song.artwork %>
<br>
<br>

<button class="btn btn-danger">
  <%= link_to "Edit Song", edit_song_path(@song.id) %>
</button>

<button class="btn btn-danger">
  <%= link_to "Delete Song", song_path(@song.id), method: :delete, data: {  confirm: "Are you sure you want to delete this song?" } %>
</button>

<button class="btn btn-danger">
  <%= link_to "All Songs", songs_path %>
</button>
